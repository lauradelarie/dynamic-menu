<h2> Pop-a-Top</h2>

<table class="toppings">

<% @toppings.each do |t| %>
  <tr>
    <td class="topping-name" id="topping-<%= t.id %>"><%= t.name %> </td>
    <td>€ <%= t.price %> </td>
    <td><input class="choose-topping-checkbox" type="checkbox"></td>
  </tr>
<% end %>

</table>
